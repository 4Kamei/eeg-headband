[target.'cfg(all(target_arch = "arm", target_os = "none"))']

runner = "xtask run"

rustflags = [
  "-C", "link-arg=-Tlink.x",
  "-C", "link-arg=-Tdefmt.x",
  "-C", "link-arg=--nmagic",
]

[build]
target = "thumbv8m.main-none-eabi"

[env]
DEFMT_LOG = "trace"
